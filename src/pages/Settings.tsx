import { PageHeading } from '../components/PageHeading.tsx';
import { Settings as SettingsIcon } from '@styled-icons/material/Settings';

export const Settings = () => {
  return (
    <>
      <PageHeading Icon={SettingsIcon} title="Settings"></PageHeading>
    </>
  );
};
